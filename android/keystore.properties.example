# Copy to keystore.properties (ignored by git) and fill in your own values.
# storeFile is relative to android/app/ — or use an absolute path.
storeFile=C:/Users/TU_USUARIO/claves/medscribe-release.jks
storePassword=
keyAlias=medscribe
keyPassword=
